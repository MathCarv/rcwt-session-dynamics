# Diagnostic provenance

`diagnosis_verified/` is the authoritative post-hoc diagnosis. It was recomputed
and verified after the archive helper's v2/v3 source-manifest check was fixed.
The earlier `diagnosis/` is retained as an intermediate artifact; its recorded
helper hash is superseded. All diagnostic facts, costs and decisions are
identical. Only that helper provenance hash differs. Neither report changes
the original experiment, its source snapshot, or its failed development screen.
